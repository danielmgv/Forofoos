<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Error</title>
    <link rel="stylesheet" href="/styles.css" />
    <style>
      main { max-width:700px; margin:3rem auto; padding:1rem; }
      pre { background:#f7f7f7; padding:1rem; border-radius:6px; overflow:auto }
    </style>
  </head>
  <body>
    <main>
      <h1>⚠️ Ha ocurrido un error</h1>
      <p><%= message %></p>

      <% if (error && error.stack) { %>
        <details>
          <summary>Detalles (solo en desarrollo)</summary>
          <pre><%= error.stack %></pre>
        </details>
      <% } %>

      <p><a href="/">Volver al inicio</a></p>
    </main>
  </body>
</html>