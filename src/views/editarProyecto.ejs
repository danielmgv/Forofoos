<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Editar Proyecto - ForoFoos</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="bg-[#121826] text-gray-200 font-sans">

  <div class="flex h-screen overflow-hidden">
    
    <!-- Sidebar -->
    <%- include('partials/sidebar') %>

    <!-- Main Content -->
    <main class="flex-1 flex flex-col h-screen overflow-hidden bg-[#121826]">
      <div class="flex-1 overflow-y-auto p-6">
        
        <div class="flex justify-between items-center mb-8">
            <div>
                <h1 class="text-2xl font-bold">Editar Proyecto</h1>
                <p class="text-gray-400 text-sm">Modifica los detalles del proyecto</p>
            </div>
            <a href="/proyectos" class="text-gray-400 hover:text-white transition flex items-center gap-2">
                <i class="fas fa-arrow-left"></i> Volver
            </a>
        </div>

        <% if (locals.error) { %>
            <div class="bg-red-900/30 border border-red-500/50 text-red-200 p-4 rounded-lg mb-6 max-w-2xl">
                <%= error %>
            </div>
        <% } %>

        <div class="bg-[#1e2532] p-6 rounded-xl border border-gray-700 max-w-2xl">
            <form action="/proyectos/editar/<%= proyecto.id %>" method="POST" class="space-y-5">
                <div>
                    <label class="block text-sm text-gray-400 mb-1">Nombre del Proyecto</label>
                    <input type="text" name="nombre" value="<%= proyecto.nombre %>" required class="w-full bg-[#121826] border border-gray-700 rounded-lg py-2.5 px-4 text-white focus:outline-none focus:border-indigo-500 transition">
                </div>
                
                <div>
                    <label class="block text-sm text-gray-400 mb-1">Descripción</label>
                    <textarea name="descripcion" rows="4" class="w-full bg-[#121826] border border-gray-700 rounded-lg py-2.5 px-4 text-white focus:outline-none focus:border-indigo-500 transition"><%= proyecto.descripcion %></textarea>
                </div>

                <div>
                    <label class="block text-sm text-gray-400 mb-1">Estado</label>
                    <select name="estado" class="w-full bg-[#121826] border border-gray-700 rounded-lg py-2.5 px-4 text-white focus:outline-none focus:border-indigo-500 transition">
                        <option value="En progreso" <%= proyecto.estado === 'En progreso' ? 'selected' : '' %>>En progreso</option>
                        <option value="Completado" <%= proyecto.estado === 'Completado' ? 'selected' : '' %>>Completado</option>
                        <option value="En espera" <%= proyecto.estado === 'En espera' ? 'selected' : '' %>>En espera</option>
                        <option value="Cancelado" <%= proyecto.estado === 'Cancelado' ? 'selected' : '' %>>Cancelado</option>
                    </select>
                </div>

                <div class="pt-4 flex gap-3">
                    <button type="submit" class="bg-indigo-600 hover:bg-indigo-500 text-white py-2.5 px-6 rounded-lg transition shadow-lg shadow-indigo-500/30 font-medium">
                        Guardar Cambios
                    </button>
                    <a href="/proyectos" class="bg-transparent border border-gray-600 hover:bg-gray-700 text-gray-300 py-2.5 px-6 rounded-lg transition font-medium">
                        Cancelar
                    </a>
                </div>
            </form>
        </div>

      </div>
    </main>
  </div>
</body>
</html>