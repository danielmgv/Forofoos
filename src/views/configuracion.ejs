<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Configuración - ForoFoos</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="bg-[#121826] text-gray-200 font-sans">

  <div class="flex h-screen overflow-hidden">
    
    <!-- Sidebar -->
    <%- include('partials/sidebar') %>

    <!-- Main Content -->
    <main class="flex-1 flex flex-col h-screen overflow-hidden bg-[#121826]">
      <div class="flex-1 overflow-y-auto p-6">
        <h1 class="text-2xl font-bold mb-2">Configuración</h1>
        <h2 class="text-gray-400 text-sm mb-8">Gestiona tu cuenta y seguridad</h2>

      <% if (locals.error) { %>
        <div class="bg-red-900/30 border border-red-500/50 text-red-200 p-4 rounded-lg mb-6 max-w-2xl">
          <%= error %>
        </div>
      <% } %>
      <% if (locals.success) { %>
        <div class="bg-green-900/30 border border-green-500/50 text-green-200 p-4 rounded-lg mb-6 max-w-2xl">
          <%= success %>
        </div>
      <% } %>

      <div class="bg-[#1e2532] p-6 rounded-xl border border-gray-700 max-w-2xl">
        <h3 class="text-lg font-bold mb-6 border-b border-gray-700 pb-2">Cambiar Contraseña</h3>
        <form action="/auth/change-password" method="POST" class="space-y-5">
          <div>
            <label class="block text-sm text-gray-400 mb-1">Contraseña Actual</label>
            <input type="password" name="currentPassword" required class="w-full bg-[#121826] border border-gray-700 rounded-lg py-2.5 px-4 text-white focus:outline-none focus:border-indigo-500 transition">
          </div>
          <div>
            <label class="block text-sm text-gray-400 mb-1">Nueva Contraseña</label>
            <input type="password" name="newPassword" required minlength="8" class="w-full bg-[#121826] border border-gray-700 rounded-lg py-2.5 px-4 text-white focus:outline-none focus:border-indigo-500 transition">
          </div>
          <div>
            <label class="block text-sm text-gray-400 mb-1">Confirmar Nueva Contraseña</label>
            <input type="password" name="confirmPassword" required minlength="8" class="w-full bg-[#121826] border border-gray-700 rounded-lg py-2.5 px-4 text-white focus:outline-none focus:border-indigo-500 transition">
          </div>
          <div class="pt-2">
            <button type="submit" class="bg-indigo-600 hover:bg-indigo-500 text-white py-2.5 px-6 rounded-lg transition shadow-lg shadow-indigo-500/30 font-medium">Actualizar Contraseña</button>
          </div>
        </form>
      </div>
      </div>
    </main>
  </div>
</body>
</html>