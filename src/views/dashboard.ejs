<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no"
    />
    <title>ForoFoos - Inicio</title>
    <style>
      :root {
        --primary: #007bff;
        --bg: #f8f9fa;
        --text: #333;
      }
      body {
        margin: 0;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto;
        background-color: var(--bg);
        color: var(--text);
        padding-bottom: 70px;
      }
      /* Cabecera superior */
      .header {
        background: white;
        padding: 15px;
        text-align: center;
        font-weight: bold;
        border-bottom: 1px solid #ddd;
        position: sticky;
        top: 0;
        z-index: 100;
      }
      /* Contenedor de posts */
      .feed {
        padding: 15px;
      }
      .post-card {
        background: white;
        border-radius: 15px;
        padding: 15px;
        margin-bottom: 15px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.05);
      }
      .post-user {
        font-weight: bold;
        font-size: 14px;
        color: var(--primary);
      }
      .post-content {
        margin-top: 8px;
        font-size: 16px;
      }

      /* Barra de navegación inferior (Estilo App) */
      .nav-bar {
        position: fixed;
        bottom: 0;
        width: 100%;
        height: 60px;
        background: white;
        border-top: 1px solid #ddd;
        display: flex;
        justify-content: space-around;
        align-items: center;
      }
      .nav-item {
        text-align: center;
        color: #777;
        text-decoration: none;
        font-size: 12px;
      }
      .nav-item.active {
        color: var(--primary);
      }
      .nav-icon {
        font-size: 20px;
        display: block;
      }
    </style>
  </head>
  <body>
    <div class="header">ForoFoos</div>

    <div class="feed">
      <% if (posts && posts.length) { %> <% posts.forEach(post => { %>
      <div class="post-card">
        <div class="post-user">@<%= post.username || 'Anónimo' %></div>
        <div class="post-content"><%= post.contenido %></div>
      </div>
      <% }) %> <% } else { %>
      <div class="post-card">
        <div class="post-user">@<%= user %></div>
        <div class="post-content">
          ¡Bienvenido a tu nueva aplicación! Aquí aparecerán las publicaciones.
        </div>
      </div>
      <% } %>

      <form action="/publicar" method="POST" class="post-card">
        <textarea
          name="contenido"
          placeholder="¿Qué estás pensando?"
          style="width: 100%; border: none; outline: none; resize: none; font-size: 16px"
        ></textarea>
        <button
          type="submit"
          style="
            background: var(--primary);
            color: white;
            border: none;
            padding: 8px 15px;
            border-radius: 20px;
            float: right;
            margin-top: 10px;
          "
        >
          Publicar
        </button>
        <div style="clear: both"></div>
      </form>
    </div>

    <nav class="nav-bar">
      <a href="/" class="nav-item active"><span class="nav-icon">🏠</span>Inicio</a>
      <a href="#" class="nav-item"><span class="nav-icon">🔍</span>Buscar</a>
      <a href="#" class="nav-item"><span class="nav-icon">🔔</span>Notis</a>
      <a href="/logout" class="nav-item"><span class="nav-icon">👤</span>Salir</a>
    </nav>
  </body>
</html>
