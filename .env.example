# Database (MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=tu_contraseña
DB_NAME=node_auth_db

# App
APP_URL=http://localhost:3000
SESSION_SECRET=un_secreto_muy_largo_y_aleatorio
PORT=3000
HOST=0.0.0.0

# Email / SMTP (optional). If not set, dev mode uses Ethereal or logs verification link.
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=username
# SMTP_PASS=password
# EMAIL_FROM="ForoFoos <no-reply@forofoos.local>"
# If your SMTP server uses a self-signed certificate, allow insecure TLS (dev only):
# SMTP_ALLOW_INSECURE=true
# Alternatively, add your CA to Node's trust store (preferred for dev):
# NODE_EXTRA_CA_CERTS=C:\\path\\to\\ca.pem   # Windows PowerShell example
# NODE_EXTRA_CA_CERTS=/path/to/ca.pem           # Linux/macOS example
# NOTE: Avoid setting `SMTP_ALLOW_INSECURE=true` in production. Use a trusted SMTP provider instead.

# Logging
# LOG_LEVEL=debug
# LOG_DIR=./logs
# LOG_MAX_SIZE=5242880